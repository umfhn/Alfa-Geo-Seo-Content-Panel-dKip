import React from 'react';

// FIX: This component has been intentionally gutted to enforce the guideline that API keys
// must be managed exclusively through environment variables (`process.env.API_KEY`).
// UI elements for entering or managing API keys are not permitted.
export const ApiKeyModal: React.FC = () => {
  return null;
};
